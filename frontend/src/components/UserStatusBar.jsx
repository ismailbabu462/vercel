import React from 'react';
import { useAuth } from '../contexts/AuthContext';
import { Badge } from './ui/badge';
import { format, differenceInDays } from 'date-fns';

const UserStatusBar = () => {
  // Don't show anything - completely removed
  return null;
};

export default UserStatusBar;
